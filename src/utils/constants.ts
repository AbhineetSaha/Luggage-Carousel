/**
 * Carousel animation constants
 */
export const CAROUSEL = {
  SPEED: 2,
  MIN_SPAWN_INTERVAL: 1500,
  MAX_SPAWN_INTERVAL: 3000,
  LUGGAGE_WIDTH: 64,
  MIN_SPACING: 150,
} as const;

/**
 * Storage grid constants
 */
export const STORAGE = {
  GRID_SIZE: 9,
  GRID_COLUMNS: 3,
  PRIORITY_ROW_SIZE: 3,
} as const;
